"use client";

// Deprecated canvas version replaced by single source of truth.
// Keeping this file as a thin re-export to avoid breaking existing imports.
// Remove this file entirely once all imports are updated to use the templates path directly.

export { FeatureSplit } from "@/templates/shadcn-components/processed/features/FeatureSplit";
export type { AuditMetric, FeatureSplitProps } from "@/templates/shadcn-components/processed/features/FeatureSplit";